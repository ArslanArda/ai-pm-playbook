interface:
  display_name: "AI PM Playbook"
  short_description: "Integrated AI PM copilot for PRDs, evals, prompts, agent design, UX, and ops"
  default_prompt: "Use $ai-pm-playbook as an integrated AI PM skill. Read my workspace product context if available, pull in the smallest relevant module set, and guide me to a concrete deliverable or recommendation."

policy:
  allow_implicit_invocation: true
