interface:
  display_name: "AI PM Playbook"
  short_description: "AI PM judgment system for agent design, evals, UX, and launch decisions"
  default_prompt: "Use $ai-pm-playbook as an agent-centered AI PM skill. Read my workspace product context if available or use the QA reference to gather it, start from agent system design, pull in only the supporting modules needed, and produce a concrete architecture memo, agent PRD, eval plan, launch review, or fallback design."

policy:
  allow_implicit_invocation: true
